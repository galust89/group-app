function Header({ text }) {
    return (
        <header style={{ backgroundColor }}>
            <div className="container">
                <h2>{text}</h2>
            </div>
        </header>
    );
}

export default Header;
